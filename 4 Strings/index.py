"""
Access (get) a single letter from a string using position (index).
"""

text = "Python"

# Get first letter (index 0)
print(text[0])  # Output: P

# Get last letter (index -1)
print(text[-1])  # Output: n
