# lambda.py - Anonymous function
# definition of a lambda function: A lambda function is a small anonymous function defined with the lambda keyword. It can take any number of arguments but can only have one expression.

square = lambda x: x * x
print(square(5))
# Output: 25